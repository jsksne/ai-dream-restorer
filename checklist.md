# Checklist（基于 Oneira Spec v3）

## 核心交互验证

- [ ] 首次生成：输入梦境描述 → Agent 追问（若模糊）→ 生成首版画面 → 伪流式动画（模糊→清晰）
- [ ] 生成中进度指示器正常显示，输入框保持可用
- [ ] 微调：直接打字补充描述 → Img2Img(strength 0.3-0.5) → 版本树横向新增节点
- [ ] 微调生成以伪流式动画展示
- [ ] 自动探索：点击画面元素 → VLM 识别 → Agent 生成方向 → Img2Img(strength 0.5-0.7)镜头拉近
- [ ] 探索画面与原图色调/风格连贯（img2img 保留氛围）
- [ ] 探索过渡动画为镜头拉近（缩放+渐变，1.5 秒内）
- [ ] 引导式探索：点元素 + 打字 → 输入框显示"→ 探索：大海" → 结合元素语义+用户描述生成
- [ ] 探索面包屑正确记录路径，可点回任意层
- [ ] 探索途中"存为新版本"→ 画面落地版本树，成为新微调起点
- [ ] 探索节点上可直接打字微调（探索路径延伸）
- [ ] 版本树始终纯横向一维展示，不与探索链混淆

## 数据结构验证

- [ ] 版本树横向链正确（VersionNode.parentId 串联）
- [ ] 探索路径纵向链正确（ExploreNode.parentExploreId 串联，关联 versionId）
- [ ] 探索存为新版本后，新版本可独立微调和探索
- [ ] 会话刷新后版本树+探索路径数据不丢失（localStorage）
- [ ] 微调链路 ≤5 层提示回退
- [ ] 探索路径 ≤7 层提示"梦境已经很深了"

## 语义标签验证

- [ ] 每次图像生成后自动调用 Qwen-VL 生成结构化标签（场景/情绪/元素）
- [ ] 标签显示在版本树节点上
- [ ] 探索节点同样生成并显示标签
- [ ] 可点元素高亮（hover 浮现元素名）
- [ ] 点击非高亮区域也可探索（Agent 对任意点击位置做 VLM 识别）

## Agent 验证

- [ ] 描述阶段：简短/模糊描述 → Agent 主动追问 1-2 问题（对话气泡）
- [ ] 用户可跳过追问直接生成
- [ ] 微调阶段：Agent 基于语义标签给构图建议（轻量提示，不强制）
- [ ] 探索阶段：Agent 调 VLM 识别元素 → 生成探索方向
- [ ] 分析阶段：Agent 调 VLM 看图 + RAG 检索

## 心理分析验证

- [ ] 仅版本树节点有"开始分析"按钮，探索节点不可标记
- [ ] 触发后基于标记版本的画面+描述
- [ ] 释梦 Agent（VLM+RAG）、情绪 Agent（VLM 画面）、睡眠 Agent（文本）三维度并行
- [ ] AI 建议串行汇总（依赖前三者）
- [ ] 前端按完成顺序流式展示（哪个先好先显示）
- [ ] 四维度有明确标题分隔
- [ ] 探索倾向辅助参考附加在分析后，明确标注"非分析主体"
- [ ] 免责声明显示

## 并行子 Agent 验证

- [ ] 图像后处理：语义标签 + 构图建议并行进行
- [ ] 心理分析三维度并行，无相互依赖污染
- [ ] 并行用 Qwen-Turbo 降本，复杂任务用 Qwen-Plus
- [ ] 首字等待时间显著缩短（目标 ~6 秒）

## 技术验证

- [ ] 硅基流动 API 密钥通过后端代理，前端不可见
- [ ] Text2Image、Img2Img（微调）、Img2Img（探索）三种模式均正常
- [ ] 图像落盘到 /public/generated/，版本树与探索节点只存本地 URL
- [ ] 云 API 临时 URL 过期后画面仍可展示
- [ ] 流式 SSE 输出正常（心理分析逐字）

## 边界情况验证

- [ ] 空输入不触发生成
- [ ] 超长描述有合理处理（截断或提示）
- [ ] 快速连续微调/探索不导致状态混乱
- [ ] 网络异常有错误提示，不导致前端崩溃
- [ ] 链路深度超限有提示

## UI/UX 验证

- [ ] 配色符合"梦境"主题（柔和、梦幻，樱花粉/薰衣草/奶白）
- [ ] 版本树交互流畅，点击切换无卡顿
- [ ] 探索镜头拉近动画自然
- [ ] 伪流式生成动画自然
- [ ] 桌面端主流浏览器兼容（Chrome/Edge/Firefox）
- [ ] 响应式适配（桌面端优先）

## 演示容灾验证

- [ ] 预置离线演示数据（缓存梦境图+分析结果）
- [ ] 网络中断时降级到离线演示模式
- [ ] 演示脚本就绪（90 秒讲完）

## 测试验证

- [ ] 已使用 Chrome DevTools MCP 全局预览测试
- [ ] 核心流程（生成→微调→探索→存为新版本→标记分析）完整通过
- [ ] 心理分析流程完整通过
- [ ] 边界情况测试通过
- [ ] 至少完成 3 轮完整测试验证
- [ ] 所有已发现问题已修复
