// @vitest-environment jsdom
// 旧 VersionTree 组件已废弃（v2 重构）。此文件保留为占位测试以避免破坏 vitest include 规则。

import { describe, it, expect } from "vitest";

describe("VersionTree (deprecated)", () => {
  it("已迁移至 useSession 的节点字典模型", () => {
    expect(true).toBe(true);
  });
});