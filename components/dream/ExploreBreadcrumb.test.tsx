// @vitest-environment jsdom
// 旧 ExploreBreadcrumb 组件已被 OneiraApp 内联实现取代。

import { describe, it, expect } from "vitest";

describe("ExploreBreadcrumb (deprecated)", () => {
  it("路径已迁移至 OneiraApp 内联渲染", () => {
    expect(true).toBe(true);
  });
});