# Oneira — AI梦境还原器 Spec（v3）

## Why

人们醒来后对梦境的记忆往往是模糊、碎片化的。现有的AI图像生成工具只能做"一次性生成"，用户无法在生成过程中逐步修正画面来逼近自己记忆中的真实梦境。Flipbook虽然支持点击探索，但它是一个无限发散的视觉浏览器——没有还原目标、没有版本管理、初始提示词不可修改，不适合"逼近记忆中的梦境"这一收敛式需求。

Oneira通过**版本树微调 + 梦中之梦探索 + 全程智能体**，让用户像"证人画像师"一样，在AI Agent的引导下不断补充描述来还原梦境画面。同时借鉴Flipbook的点击探索交互，但将其改造为"梦中之梦"——点击画面元素可镜头拉近进入该元素的子梦境，视觉连贯、层层下潜，契合梦境的联想本质。在生成结束后，用户主动标记最接近记忆的画面，获得基于多模态图像理解的心理分析。

## What Changes

- 新建项目：Oneira — AI梦境还原器
- 核心功能：基于云端图像生成API的梦境画面生成（伪流式渲染）
- 核心创新一：版本树微调机制 — 用户补充描述，基于当前画面做Img2Img微调（而非重新生成），形成横向一维版本链
- 核心创新二：梦中之梦探索 — 用户点击画面任意元素，Agent用VLM识别元素后做img2img镜头拉近，进入该元素的子画面，形成可回溯的纵向探索路径（面包屑）
- 分离架构：版本树（横向，收敛式还原）与探索链（纵向，联想式下潜）在空间和交互上彻底分离，互不干扰
- AI深度核心：全程贯穿的智能体（Agent）— 在描述阶段追问细节、微调阶段给构图建议、探索阶段识别元素并引导、分析阶段调用释梦RAG做多模态解读
- 附加功能：多模态心理分析模块（VLM看图 + 释梦理论RAG + 情绪分析 + 睡眠质量 + AI建议），用户主动标记分析对象后流式输出
- 技术路线：前端Web应用（React/Next.js） + 后端API代理 + 硅基流动API（FLUX/SD图像生成 + Qwen-VL多模态 + Qwen-Plus Agent）

## Impact

- Affected specs: 无（新建项目）
- Affected code: 全新项目，无已有代码受影响

## 核心交互模型

### 两种行为，空间分离

Oneira的交互围绕两种方向完全不同的行为展开，它们在界面上被彻底分离，避免心智模型混淆：

| 行为 | 方向 | 目标 | 界面位置 | 数据结构 |
|------|------|------|----------|----------|
| 微调 | 横向 | 在同一画面上改细节，逼近记忆中的梦 | 底部版本树（一维时间轴） | 版本链（线性） |
| 探索 | 纵向 | 从当前画面潜入元素的子梦境 | 画面区（沉浸式）+ 面包屑 | 探索路径（链式） |

### 界面布局

```
┌─────────────────────────────────────────────┐
│  画面区（沉浸式探索在这里发生）                  │
│                                              │
│  当前梦境画面                                  │
│  ┌─ 可点元素高亮 ─┐                           │
│  │  大海  月亮     │  ← 点击元素 = 镜头拉近探索  │
│  └────────────────┘                          │
└─────────────────────────────────────────────┘
  探索面包屑: 初版 › 大海 › 小鱼  [←返回各层]
┌─────────────────────────────────────────────┐
│  版本树（纯横向一维，只管微调）                  │
│  ●━━━●━━━●━━━●                              │
│  初版  二版  三版  四版   ← 始终清晰不迷路       │
└─────────────────────────────────────────────┘
  底部输入框: [补充描述微调当前画面...]
```

### 统一输入语法

底部常驻一个输入框，通过"是否先点元素"区分三种行为：

- **只打字**（如"加暴风雨"）→ 对当前画面做微调（横向新增版本节点）
- **只点元素**（如点大海）→ Agent用VLM识别元素后自动探索（纵向新增探索节点），用户无需打字
- **先点元素 + 再打字**（点大海 + "往深海沉"）→ 引导式探索，用户给方向，Agent结合元素语义生成新画面

交互细节：点击某元素后该元素高亮选中，输入框显示上下文提示"→ 探索：大海"，让用户知道此时打字是在引导该元素的探索；直接回车则自动探索。三种行为共用一个入口，学习成本低。

### 探索与微调的连接

探索途中如果觉得"这个画面才像我记忆中的梦"，用户点"存为新版本"按钮 → 当前探索画面落地到版本树，成为新的微调起点（版本树新增节点）。不存就只是玩，不影响版本树。反之，版本树的任意节点都可以作为探索的起点——点击画面元素即可开始下潜。

### 心理分析对象

心理分析的主对象由**用户主动标记**——用户在版本树的任意节点点"开始分析"按钮。这样"哪个画面和潜意识有关"的判断由最了解自己内心的用户做出，系统不越俎代庖。只有版本树里的节点（用户有意还原的梦）可被标记为分析对象；纯探索节点不可标记（探索可能只是好奇，不构成可靠的梦境材料）。Agent在分析时可附加提及用户的探索倾向作为辅助参考（如"你在探索中多次接近水的意象"），但不作为分析主体。

## ADDED Requirements

### Requirement: 梦境图像生成（伪流式渲染）

系统 SHALL 提供基于文字描述的图像生成功能。用户输入梦境描述后，AI生成画面并在前端以伪流式方式展示（从模糊到清晰的渐进动画）。

> **技术说明：** 硅基流动/Replicate/Stability AI的图像生成API均只返回最终图片，不提供去噪中间帧。因此采用**伪流式方案**：拿到最终图后，前端用CSS blur + 逐块reveal做"从模糊到清晰"的渐进动画，配合进度条。视觉上接近真流式效果，零额外成本、零额外依赖。如有GPU资源可升级为真流式（自托管ComfyUI + WebSocket step callback，preview image节点逐帧推送去噪中间帧）。

#### Scenario: 首次生成
- **WHEN** 用户输入梦境描述（如"梦见飞过大海"）并点击生成
- **THEN** 系统调用云端图像生成API（Text2Image模式，FLUX/SD系列）
- **AND** 前端显示"生成中"进度指示器（脉冲动画 + "AI正在描绘你的梦境..."提示）
- **AND** 图像返回后，前端以CSS blur渐进动画展示（从模糊到清晰，3秒内完成）
- **AND** 生成过程中用户输入框保持可用状态

#### Scenario: 生成中等待
- **WHEN** 图像正在生成中（API请求已发送，等待返回）
- **THEN** 前端显示进度指示器
- **AND** 用户输入框保持可用状态（用户可随时准备补充描述）

### Requirement: 版本树微调机制（横向收敛）

系统 SHALL 提供版本树管理功能。用户在当前画面基础上补充描述后，系统基于当前画面做Img2Img微调生成新版本，形成横向一维版本链。版本树始终以纯横向时间轴展示，不与探索链混淆。

#### Scenario: 基于当前版本微调
- **WHEN** 用户在输入框直接打字补充描述（如"加暴风雨"），未先点击任何元素
- **THEN** 系统将当前画面 + 新描述作为输入，通过Img2Img生成新版本
- **AND** 降噪强度（strength）设置为0.3-0.5，保留原图结构只改细节
- **AND** 新版本作为当前版本的下一个节点，添加到版本树横向链中
- **AND** 前端以伪流式动画展示微调生成过程

#### Scenario: 版本切换
- **WHEN** 用户点击版本树中的任意版本节点
- **THEN** 前端切换展示该版本的画面
- **AND** 当前选中版本高亮显示
- **AND** 若该版本有探索路径，面包屑重置到该版本

#### Scenario: 链路深度限制
- **WHEN** 用户从根版本开始的微调链路深度超过5层
- **THEN** 系统提示"当前链路过深，细节可能漂移，建议从较早版本重新开始以获得更好效果"
- **AND** 用户可选择继续或回退到较早版本

#### Scenario: 版本节点信息展示
- **WHEN** 用户查看版本树
- **THEN** 每个版本节点显示对应的用户描述（类似对话轮次）
- **AND** 每个版本节点展示AI自动生成的语义标签（场景/情绪/元素）
- **AND** 每个版本节点有"开始分析"按钮，供用户标记心理分析对象

### Requirement: 梦中之梦探索（纵向联想）

系统 SHALL 提供基于画面元素的探索功能。用户点击画面中任意元素后，Agent用VLM识别该元素，基于当前画面做Img2Img镜头拉近，生成该元素的子画面，形成可回溯的纵向探索路径。

#### Scenario: 自动探索（点击元素）
- **WHEN** 用户点击画面中某个元素（如大海）
- **THEN** Agent调用Qwen-VL识别点击区域的元素语义（如"海洋"）
- **AND** Agent基于元素语义自动生成探索方向描述（如"沉入深海，光线穿过水面"）
- **AND** 系统将当前画面 + 探索描述作为输入，通过Img2Img（strength 0.5-0.7）生成镜头拉近的新画面
- **AND** 新画面视觉上与当前画面色调/风格连贯（img2img保留原图氛围）
- **AND** 前端以过渡动画展示镜头拉近效果（缩放+渐变）
- **AND** 面包屑新增该探索节点（如"初版 › 大海"）

#### Scenario: 引导式探索（点击元素 + 补充描述）
- **WHEN** 用户先点击某元素（如大海），再在输入框打字补充方向（如"往深海沉"）
- **THEN** 输入框显示上下文提示"→ 探索：大海"
- **AND** Agent结合元素语义 + 用户描述生成新画面
- **AND** 其余流程同自动探索

#### Scenario: 探索路径回溯
- **WHEN** 用户点击面包屑中的任意历史探索节点
- **THEN** 前端切换展示该探索节点的画面
- **AND** 面包屑高亮当前位置
- **AND** 用户可从该节点继续探索或返回

#### Scenario: 探索画面存为新版本
- **WHEN** 用户在探索途中点击"存为新版本"按钮
- **THEN** 当前探索画面落地到版本树，作为新的微调起点
- **AND** 版本树横向链新增节点
- **AND** 后续微调基于该新版本进行

#### Scenario: 探索节点继续微调
- **WHEN** 用户在某个探索节点画面上，直接打字补充描述（未点元素）
- **THEN** 系统对该探索画面做Img2Img微调
- **AND** 微调结果作为该探索节点的延伸（探索路径上的新节点）
- **AND** 用户可选择"存为新版本"将其落地到版本树

### Requirement: 图像语义标签（AI增强版本树）

系统 SHALL 在每次图像生成完成后，自动调用VLM对画面进行语义分析，生成结构化标签，让版本树从工程结构升级为AI增强的语义结构。

#### Scenario: 自动打标签
- **WHEN** 图像生成完成并落盘存储后
- **THEN** 系统调用Qwen-VL对图像进行语义分析
- **AND** 生成结构化标签：场景类型（如"海洋/天空/室内"）、情绪基调（如"宁静/紧张/神秘"）、关键元素（如"船/月亮/黑影"）
- **AND** 标签显示在版本树对应节点上
- **AND** 探索节点同样生成语义标签，显示在面包屑旁

#### Scenario: 可点元素高亮（探索引导）
- **WHEN** 图像生成完成并打标签后
- **THEN** 系统基于语义标签识别画面中可点击探索的元素区域
- **AND** 可点元素以微妙高亮提示（hover时浮现元素名称）
- **AND** 用户点击任意位置均可探索（不限于高亮区域，Agent对任意点击位置做VLM识别）

### Requirement: 全程贯穿的智能体（Agent）

系统 SHALL 集成一个智能体，贯穿梦境还原的全流程，提供AI深度的核心能力。Agent基于Qwen-Plus实现轻量级ReAct loop，在四个阶段提供不同能力。

#### Scenario: 描述阶段 — 主动追问
- **WHEN** 用户输入的初始梦境描述过于简短或模糊（少于10个字或缺少场景细节）
- **THEN** Agent主动追问1-2个问题（如"梦境中的光线是明还是暗？""你当时的情绪是恐惧还是好奇？"）
- **AND** 追问以对话气泡形式显示在输入区上方
- **AND** 用户可选择回答或跳过直接生成

#### Scenario: 微调阶段 — 构图建议
- **WHEN** 用户补充描述进行微调时
- **THEN** Agent基于当前画面语义标签，给出1条构图建议（如"如果加暴风雨，建议降低画面亮度以增强压迫感"）
- **AND** 建议以轻量提示形式显示，不强制接受

#### Scenario: 探索阶段 — 元素识别与引导
- **WHEN** 用户点击画面元素触发探索
- **THEN** Agent调用Qwen-VL识别点击区域元素语义
- **AND** Agent基于元素语义 + 当前画面氛围生成探索方向描述
- **AND** 若为引导式探索，Agent结合用户补充描述优化探索方向

#### Scenario: 分析阶段 — 多模态释梦
- **WHEN** 用户在版本树节点点击"开始分析"
- **THEN** Agent调用Qwen-VL对标记画面进行视觉理解
- **AND** 结合画面视觉特征（色彩/构图/元素）+ 用户描述文本 + 释梦知识库（RAG）
- **AND** 生成四维度心理分析，流式输出

### Requirement: 多模态心理分析模块

系统 SHALL 在用户主动标记分析对象后，提供流式心理分析输出。分析基于用户标记的版本画面（VLM视觉理解）+ 描述文本 + 释梦理论RAG。心理分析的主对象是版本树中用户有意还原的梦境，而非随机探索的产物。

#### Scenario: 触发心理分析
- **WHEN** 用户在版本树的某个节点点击"开始分析"按钮
- **THEN** 系统将该版本的画面图像 + 描述文本作为输入
- **AND** Agent调用Qwen-VL进行多模态视觉理解
- **AND** 检索释梦知识库（RAG）获取相关理论参考
- **AND** 分析结果在前端流式输出（逐字显示）

#### Scenario: 分析内容组成（四维度）
- **WHEN** 心理分析生成中
- **THEN** 输出内容包含以下四个维度：
  1. 释梦解读（参考弗洛伊德/荣格等流派视角，基于RAG检索结果，不要求精确引用出处）
  2. 情绪分析（VLM基于梦境画面的色彩、构图、元素分析情绪状态）
  3. 睡眠质量评估（基于梦境内容推断可能的睡眠质量参考）
  4. AI建议（基于以上分析给出改善建议）
- **AND** 每个维度有明确的标题分隔

#### Scenario: 探索倾向辅助参考
- **WHEN** 心理分析输出完成后
- **THEN** 若用户在本次会话中有探索行为，Agent附加一段探索倾向观察
- **AND** 观察仅作为兴趣参考（如"你在探索中多次接近水的意象，这可能值得留意"），明确标注为"探索倾向参考，非分析主体"
- **AND** 不将探索画面作为释梦依据

#### Scenario: 免责声明
- **WHEN** 心理分析输出完成
- **THEN** 页面底部显示免责声明："本分析仅供娱乐参考，不构成医学诊断或心理咨询建议。如有睡眠或心理困扰，请咨询专业医生。"

### Requirement: 图像持久化

系统 SHALL 将所有生成的图像落盘存储到本地，版本树和探索路径只存储本地URL，不依赖云API返回的临时URL。

#### Scenario: 图像落盘
- **WHEN** 云端API返回图像后
- **THEN** 后端立即下载图像并存储到本地/public/generated/目录
- **AND** 版本树节点和探索节点均记录本地URL（如/generated/dream_001.png）
- **AND** 即使云API的临时URL过期，版本树和探索路径画面仍可正常展示

### Requirement: 并行智能体加速（可选优化）

系统 MAY 在心理分析和图像后处理阶段引入并行子智能体，加速响应。基于智能体蜂群研究的启示：可扩展性更多来自上下文效率而非并行性本身，因此仅在任务相互独立、无依赖时启用并行。

> **设计依据：** 参考Cursor智能体蜂群研究——规划器（强模型）负责拆分委派，worker（低成本模型）负责执行，每个worker只关注自己的窄上下文。Oneira的任务规模中等、为实时交互应用，不需要完整蜂群架构，但在心理分析多维度生成和图像后处理两个场景，并行可显著缩短用户等待时间。

#### Scenario: 心理分析三维度并行
- **WHEN** 用户触发心理分析
- **THEN** 系统并行启动三个子Agent，分别生成"释梦解读""情绪分析""睡眠质量评估"
- **AND** 三个子Agent各自只携带自己维度的窄上下文（释梦Agent只看RAG+画面，情绪Agent只看画面视觉特征，睡眠Agent只看描述文本）
- **AND** 三个维度结果汇总后，由主Agent串行生成"AI建议"（依赖前三者）
- **AND** 前端按各维度完成顺序流式展示（哪个先好先显示）

#### Scenario: 图像后处理并行
- **WHEN** 图像生成完成并落盘后
- **THEN** 系统并行启动两个子任务：Qwen-VL生成语义标签 + Agent生成构图建议
- **AND** 两者互不依赖，可同时进行
- **AND** 结果分别写入版本树节点和Agent建议区

## 技术架构

### 模型选型

| 用途 | 模型 | 平台 | 理由 |
|------|------|------|------|
| 图像生成（Text2Image） | FLUX/SD系列 | 硅基流动 | 国内稳定，成本低，FLUX schnell免费 |
| 图像微调（Img2Img） | FLUX/SD系列 | 硅基流动 | 保持一致性，支持设置降噪强度 |
| 图像编辑（区域探索） | Qwen-Image-Edit-2509 | 硅基流动 | 支持图像编辑/inpainting，元素级操作 |
| 多模态视觉理解 | Qwen-VL | 硅基流动 | 原生支持看图分析，元素识别，国内直连 |
| Agent（ReAct loop） | Qwen-Plus | 硅基流动 | 指令遵循能力强，成本可控 |
| 并行子Agent | Qwen-Plus / Qwen-Turbo | 硅基流动 | 窄任务用Turbo降本，复杂任务用Plus |
| 释梦RAG知识库 | 本地向量库 | - | 预置释梦理论文档，Embedding检索 |

### 前端

- Web应用（React/Next.js）
- **画面区**：沉浸式展示当前梦境画面，支持元素点击探索，镜头拉近过渡动画
- **探索面包屑**：顶部横向面包屑，记录探索路径，可点回任意层
- **版本树**：底部纯横向一维时间轴，只管微调链，始终清晰不迷路
- **底部输入框**：统一入口，通过"是否先点元素"区分微调/探索/引导探索
- 伪流式图像渲染组件（CSS blur + 逐块reveal渐进动画 + 进度条）
- 流式文字渲染组件（心理分析逐字输出）
- Agent对话组件（追问/建议的对话气泡展示）

### 后端

- API代理层（隐藏硅基流动API密钥，处理请求转发）
- 图像生成模块（Text2Image + Img2Img + 图像编辑，支持设置降噪强度）
- Qwen-VL调用模块（图像语义标签 + 元素识别 + 多模态心理分析）
- Agent ReAct loop模块（追问/建议/探索引导/分析编排）
- 并行子Agent调度模块（心理分析三维度并行 + 图像后处理并行）
- 释梦RAG模块（本地向量库检索）
- 图像落盘模块（下载云API返回图像到本地）
- 数据管理模块（版本树横向链 + 探索路径纵向链，分离存储）

### 数据结构

```typescript
// 版本树节点（横向微调链）
interface VersionNode {
  id: string;
  prompt: string;           // 用户描述
  imageUrl: string;         // 本地图像URL
  parentId: string | null;  // 父版本（横向链）
  semanticTags: {
    scene: string;          // 场景类型
    emotion: string;        // 情绪基调
    elements: string[];     // 关键元素
  };
  createdAt: number;
}

// 探索节点（纵向探索路径）
interface ExploreNode {
  id: string;
  versionId: string;        // 所属版本（探索起点）
  parentExploreId: string | null; // 父探索节点（纵向链）
  element: string;          // 点击的元素（如"大海"）
  explorePrompt: string;    // Agent生成的探索方向描述
  imageUrl: string;         // 本地图像URL
  semanticTags: SemanticTags;
  createdAt: number;
}

// 会话状态
interface SessionState {
  versionTree: VersionNode[];   // 横向版本链
  explorePaths: ExploreNode[][]; // 每个版本的探索路径（纵向链数组）
  currentVersionId: string;
  currentExplorePath: ExploreNode[] | null; // 当前活跃的探索路径
  analysisTargetId: string | null; // 用户标记的分析对象
}
```

### 外部依赖

- 硅基流动API（图像生成 + Qwen-VL + Qwen-Plus + Qwen-Image-Edit-2509）
- 本地向量库（用于释梦知识库RAG，如sentence-transformers）
- 释梦理论文档（预置的弗洛伊德/荣格理论摘要）

### 数据流

```
用户输入描述
     ↓
Agent判断是否需要追问 ──是──→ Agent追问 → 用户回答/跳过
     ↓ 否
后端API代理 → 硅基流动Text2Image → 返回最终图像
     ↓                                ↓
图像落盘到本地 ←─────────────────────┘
     ↓
[并行] Qwen-VL生成语义标签  +  Agent生成构图建议
     ↓
版本树记录节点（图URL+描述+标签）→ 前端伪流式渲染
     ↓
═══════════ 用户可进入两种行为 ═══════════

【微调路径】用户直接打字 → Img2Img(strength=0.3-0.5) → 落盘 → 标签 → 版本树横向新增节点

【探索路径】用户点击元素 → Qwen-VL识别元素 → Agent生成探索方向
     → Img2Img(strength=0.5-0.7)镜头拉近 → 落盘 → 标签 → 探索路径纵向新增节点 → 面包屑更新
     → 可继续探索 / 可存为新版本落地到版本树 / 可回溯

═══════════ 用户标记分析对象 ═══════════
用户在版本树节点点"开始分析"
     ↓
[并行] 释梦Agent(VLM+RAG) + 情绪Agent(VLM画面) + 睡眠Agent(文本)
     ↓ 三者汇总
主Agent串行生成AI建议
     ↓
流式输出四维度心理分析 + 探索倾向辅助参考
     ↓
显示免责声明
```

## 非功能需求

- **响应速度**：图像生成API调用后，前端立即显示进度指示器；图像返回后伪流式动画在3秒内完成；探索过渡动画在1.5秒内完成
- **可用性**：比赛演示场景下，确保网络畅通即可正常使用；预置一套离线演示数据（缓存梦境+分析结果）作为网络中断容灾
- **成本控制**：开发阶段使用硅基流动API，FLUX schnell免费；控制单次img2img生成成本在0.1元以内；Qwen-VL标签生成单次约0.01元；并行子Agent用Qwen-Turbo降本
- **兼容性**：前端支持主流浏览器（Chrome/Edge/Firefox），响应式适配桌面端优先
- **链路深度**：Img2Img微调链路建议≤5层，超过后提示用户回退；探索路径建议≤7层，超过后提示"梦境已经很深了"
- **数据持久化**：会话级存储（localStorage），无账号体系；图像落盘到本地，不依赖云API临时URL
- **合规性**：心理分析必须附带免责声明，不构成医学诊断

## MVP边界

- **MVP必须**：
  - 伪流式图像生成 + 版本树微调（横向收敛闭环）
  - 梦中之梦探索（纵向联想，点击元素→img2img镜头拉近→面包屑回溯）
  - 探索画面存为新版本（探索与微调的连接）
  - 全程Agent（追问 + 构图建议 + 探索引导 + 多模态分析）
  - 图像语义标签（AI增强版本树 + 可点元素高亮）
  - 多模态心理分析（VLM看图 + RAG，用户主动标记分析对象）
  - 图像落盘持久化
  - 免责声明
  - 演示容灾（离线缓存数据）
- **MVP可选优化**：
  - 并行子Agent加速（心理分析三维度并行 + 图像后处理并行）
- **后续迭代**：
  - 真流式生成（自托管ComfyUI + WebSocket，需GPU资源）
  - 多模态输入（语音/手绘）
  - 梦境社区分享
  - 账号体系与跨设备同步
  - 完整蜂群架构（规划器+worker树状分解，用于复杂分析任务）

## 关于并行SubAgent的设计决策

### 是否需要完整的智能体蜂群？

**不需要。** 参考Cursor的智能体蜂群研究：蜂群架构的核心价值在于"上下文效率"——规划器上下文不被底层细节塞满，worker全部上下文投入小块工作。这种优势在**长时间运行的超大规模任务**（如从零构建浏览器、用Rust实现SQLite）中显著。但Oneira的特点是：

- 任务规模中等（每个子任务都是独立的短任务，非超大规模分解）
- 实时交互应用，用户在场等待，协调延迟会降低体验
- 单Agent的上下文效率问题不严重（每个任务都很短，不会跑偏）

因此完整蜂群架构（规划器+worker树状分解+自定义VCS+冲突解决）对Oneira属于过度设计。

### 适度并行的两个场景

基于蜂群研究的启示——"在任务相互独立、无依赖时启用并行"——Oneira在两个场景引入轻量并行：

1. **心理分析三维度并行**：释梦、情绪、睡眠三个维度相互独立，可并行生成；AI建议依赖前三者，串行汇总。借鉴蜂群的"窄上下文"思想，每个子Agent只携带自己维度的上下文，避免单Agent上下文被四个维度塞满。
2. **图像后处理并行**：语义标签和构图建议互不依赖，生成后可同时进行。

### 不并行的场景

- Agent追问、探索引导等交互式任务：用户在场实时等待，串行更可控
- 版本树和探索路径管理：纯前端状态，无API调用，无需并行
- 图像生成本身：单次API调用，无法并行加速

### 成本与复杂度权衡

并行子Agent会增加API调用次数（心理分析从1次变3+1次），但使用Qwen-Turbo执行窄任务可控制成本。协调复杂度通过"无依赖才并行"原则控制——不引入蜂群的冲突解决、VCS等重型机制。比赛演示中，并行能显著缩短心理分析的首字等待时间（从串行~15秒降到并行~6秒），体验提升明显。
