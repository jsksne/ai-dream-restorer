# Oneira 须知信息与全链路指南

> 本文件分两部分：第一部分是发给 Code 模型的须知信息；第二部分是全链路需要你亲自操作的指南（注册、配置、准备材料）。

---

## 第一部分：发给 Code 模型的须知信息

把 spec_v3、tasks_v3、checklist_v3 三个文件连同以下须知一起发给 Code 模型：

### 1. 项目性质与约束

- 这是一个参加 **AI 应用比赛**的项目，评分看重：AI 技术深度、应用创新性、体验完整度、可运行性。
- 用户本机是 **ThinkPad，无 NVIDIA 显卡**，因此图像生成必须走**云 API**，不能自托管 SD/ComfyUI。spec 已写明伪流式方案，不要尝试本地推理。
- 技术栈：**Next.js（前端 + API Routes 后端）+ Tailwind CSS**。用户偏好 React/Vite/GSAP，本项目用 Next.js 是因为需要 API Routes 做后端代理。

### 2. API 与模型（全部用硅基流动 SiliconFlow）

所有模型走硅基流动一个平台，API 兼容 OpenAI 格式：

| 用途 | 模型 ID（以硅基流动文档为准） | 说明 |
|------|------|------|
| 图像生成 Text2Image | FLUX.1-schnell / FLUX.1-dev | schnell 免费，dev 质量更好 |
| 图像微调 Img2Img | FLUX 系列（支持图生图） | 微调 strength 0.3-0.5，探索 strength 0.5-0.7 |
| 多模态视觉理解 | Qwen-VL-Plus / Qwen-VL-Max | 语义标签、元素识别、心理分析看图 |
| Agent ReAct loop | Qwen-Plus | 指令遵循强，成本可控 |
| 并行子 Agent（窄任务） | Qwen-Turbo | 降本 |

- **API 密钥放后端环境变量**，前端绝对不可见。Code 模型实现时用 `process.env.SILICONFLOW_API_KEY`。
- 图像生成接口返回的是图片 URL（有时效），**必须立即下载落盘**到 `/public/generated/`，版本树和探索节点只存本地 URL。

### 3. 不要做的事

- **不要用腾讯元器、Coze、Dify 等智能体平台**。Oneira 的 Agent 是后端代码里用 ReAct loop 调用 Qwen-Plus API 实现的，不是平台托管的智能体。用平台会受编排约束、无法自定义并行子 Agent、无法集成流式 SSE 到前端。
- **不要尝试真流式图像生成**（自托管 SD）。用户无 GPU，spec 已写明伪流式（CSS blur 渐进动画），照做即可。
- **不要把心理分析写成一个 LLM 调用产出四维度**。spec 要求释梦/情绪/睡眠三维度并行子 Agent + AI 建议串行汇总，这是 AI 深度的得分点。
- **不要让 LLM 精确引用弗洛伊德/荣格出处**，改为"参考流派视角"，避免幻觉。释梦理论用本地 RAG 检索。
- **不要把探索节点作为心理分析对象**。只有版本树节点（用户有意还原的梦）可标记分析；探索只是好奇，不构成可靠梦境材料。

### 4. 核心交互模型（务必理解）

横纵分离是本项目的核心设计，务必吃透：

- **横向 = 微调**（版本树，底部一维时间轴）：用户直接打字 → Img2Img 改细节 → 版本链新增。收敛式，逼近记忆中的梦。
- **纵向 = 探索**（画面区 + 面包屑）：用户点元素 → VLM 识别 → Img2Img 镜头拉近 → 探索路径新增。联想式，梦中之梦。
- **统一输入框**：只打字=微调，只点元素=自动探索，点元素+打字=引导探索。
- **探索存为新版本**：探索画面可落地版本树，成为新微调起点。
- **用户主动标记分析对象**：版本树节点有"开始分析"按钮。

### 5. 流式实现

- 心理分析用 SSE 流式输出（逐字显示）。硅基流动 API 兼容 OpenAI 格式，`stream: true` 即可。
- 图像生成的"伪流式"是前端 CSS 动画，不是 API 流式。
- 并行子 Agent 的结果按完成顺序流式展示（哪个先好先显示）。

### 6. 测试要求

- 用 Chrome DevTools MCP 做全局预览测试。
- 至少 3 轮完整测试，覆盖：首次生成→微调→探索→存为新版本→标记分析→心理分析。
- 边界：空输入、超长描述、快速连续操作、网络异常、链路深度超限。
- 修复所有问题后再交付。

### 7. 环境变量模板（告诉 Code 模型需要这些）

```env
SILICONFLOW_API_KEY=sk-xxxxxxxxxxxx
# 图像模型
IMAGE_MODEL=black-forest-labs/FLUX.1-schnell
# VLM
VLM_MODEL=Qwen/Qwen2.5-VL-72B-Instruct
# Agent
AGENT_MODEL=Qwen/Qwen-Plus
# 子Agent（降本）
SUB_AGENT_MODEL=Qwen/Qwen-Turbo
```
（具体模型 ID 以硅基流动控制台模型列表为准，Code 模型应先查阅硅基流动文档确认可用 ID）

---

## 第二部分：全链路你需要做什么（详细指南）

### 一句话回答你的核心疑问

> **不需要注册"智能体"，不需要用元器。** Oneira 的"智能体"是你后端代码里用 ReAct loop 调用硅基流动 Qwen-Plus API 实现的程序逻辑，不是在某个智能体平台上创建的托管实体。你只需要在**硅基流动**注册一个账号、实名认证、拿到 API Key，配置到项目里就够了。

### 第 1 步：注册硅基流动账号并拿 API Key（必做）

这是唯一需要注册的平台。硅基流动（SiliconFlow）是 spec 选定的模型提供方，提供 FLUX 图像生成、Qwen-VL 多模态、Qwen-Plus/Turbo 文本模型，API 兼容 OpenAI 格式。

1. **访问官网**：打开 `https://cloud.siliconflow.cn`，用手机号或微信注册。
2. **实名认证**：注册后在控制台完成实名认证（个人实名即可，必须做，否则无法调用 API）。
3. **领取额度**：新用户注册通常送 14 元额度，够开发测试用。
4. **创建 API Key**：登录后，左侧菜单点"API 密钥" → "新建 API 密钥" → 复制密钥（形如 `sk-xxxxxxxx`）。
5. **确认模型可用**：在控制台"模型广场"确认以下模型可用：
   - FLUX.1-schnell（免费）或 FLUX.1-dev（图像生成）
   - Qwen-VL 系列（多模态）
   - Qwen-Plus、Qwen-Turbo（文本/Agent）
6. **保管密钥**：这个 Key 是项目的核心凭证，不要泄露、不要提交到 git（放 `.env.local`，`.gitignore` 里加上）。

### 第 2 步：确认是否需要额外充值（按需）

- 新用户 14 元额度 + FLUX-schnell 免费，开发阶段基本够用。
- 如果开发中额度耗尽（img2img 付费、Qwen-VL 按 token 计费），在硅基流动控制台充值 20-50 元即可完成整个比赛开发+演示。
- Qwen-Turbo 很便宜，并行子 Agent 用它降本。

### 第 3 步：准备释梦 RAG 知识库文档（必做，Code 模型无法替你做）

心理分析的释梦维度需要本地 RAG 知识库。你需要准备文本素材（Code 模型负责搭建向量库和检索，但素材要你提供或确认）：

1. **弗洛伊德释梦理论摘要**：《梦的解析》核心观点——梦是愿望的满足、显性梦境与隐性梦念、梦的象征、凝缩与移置。整理成 3-5 段结构化文本。
2. **荣格原型理论摘要**：集体无意识、原型（阴影、阿尼玛/阿尼姆斯、自性）、梦的补偿功能、个体化进程。整理成 3-5 段。
3. **常见梦境意象参考**：水、飞翔、坠落、追逐、牙齿脱落等常见梦境意象的心理学解读（可作为补充检索材料）。
4. 放到项目 `data/dream_theory/` 目录，Code 模型会用 sentence-transformers 做 Embedding 建库。

> 注意：这些内容要求是"参考流派视角"，不要求精确引用出处，所以摘要即可，不必逐字摘抄原典。

### 第 4 步：关于元器（明确不需要）

你问"元器可以吗"——**不建议用，本项目不需要**。原因：

| 维度 | 腾讯元器 | Oneira 需求 |
|------|---------|------------|
| 底层模型 | 腾讯混元 | spec 选定 Qwen 系列（硅基流动） |
| 定位 | 低代码智能体创作与分发平台 | 需要自定义 ReAct loop、并行子 Agent、流式 SSE |
| 分发 | 面向 QQ/微信生态 | 独立 Web 应用，不需要生态分发 |
| 图像能力 | 图像创作引擎（通用） | 需要 FLUX img2img + strength 精控 |
| 灵活性 | 受平台编排约束 | 需要深度定制 Agent 行为 |

元器适合"零代码搭一个能聊天的智能体挂到微信"，不适合 Oneira 这种需要精细控制图像生成流程、多模态分析、并行调度的自建应用。Oneira 的 Agent 是代码逻辑，不是平台实体。

### 第 5 步：准备演示容灾数据（比赛前必做）

现场演示最怕网络中断或 API 限流导致全程卡死。你需要：

1. 开发完成后，跑一遍完整流程（生成→微调→探索→分析），把生成的图像和分析结果保存下来。
2. Code 模型会实现"离线演示模式"——网络中断时降级用缓存数据演示。
3. 你确认这套缓存数据能完整走完演示流程。

### 第 6 步：本地环境准备

1. **Node.js**：安装 18+ 版本（Next.js 要求）。
2. **pnpm 或 npm**：包管理器。
3. **编辑器**：VS Code 或你顺手的。
4. **浏览器**：Chrome（开发调试 + DevTools 测试）。

### 全链路清单（打勾确认）

- [ ] 硅基流动账号已注册
- [ ] 实名认证已完成
- [ ] API Key 已创建并妥善保管
- [ ] 控制台确认 FLUX / Qwen-VL / Qwen-Plus / Qwen-Turbo 可用
- [ ] 额度充足（必要时充值）
- [ ] 释梦理论文档素材已整理（弗洛伊德/荣格/常见意象）
- [ ] Node.js 18+ 已安装
- [ ] （开发完成后）演示容灾数据已准备
- [ ] （比赛前）演示脚本（90 秒流程）已熟悉

做完以上这些，把三个文件（spec_v3、tasks_v3、checklist_v3）+ 本须知文件发给 Code 模型，它就能开始开发了。
